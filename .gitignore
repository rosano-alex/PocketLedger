node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local

# Runtime ledger data; the app recreates this on first write.
server/data/transactions.json
server/data/*.tmp

# Expo's local build cache.
.expo/
mobile/.expo/
